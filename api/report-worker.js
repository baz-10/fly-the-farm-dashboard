const crypto=require('crypto');const{processNextReportJob}=require('../server/report-worker');
function valid(received,expected){const a=Buffer.from(String(received||'')),b=Buffer.from(String(expected||''));return a.length===b.length&&a.length>0&&crypto.timingSafeEqual(a,b);}
module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({error:{code:'METHOD_NOT_ALLOWED',message:'Method not allowed.'}});if(!valid(req.headers?.authorization?.replace(/^Bearer\s+/i,''),process.env.REPORT_WORKER_TOKEN))return res.status(401).json({error:{code:'UNAUTHENTICATED',message:'Worker authentication required.'}});const result=await processNextReportJob({workerId:`vercel-${crypto.randomUUID()}`});return res.status(200).json({processed:result.processed,jobId:result.jobId||null,failed:Boolean(result.failed)});};
