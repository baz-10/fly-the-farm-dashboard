import{render,screen}from'@testing-library/react';import CasaComplianceOverview from'../CasaComplianceOverview';
jest.mock('../../services/complianceApi',()=>({createComplianceApi:()=>({overview:jest.fn().mockResolvedValue({evaluatedAt:'2026-08-05T00:00:00Z',reoc:{instrument_number:'CASA.REOC.123',expiry_date:'2027-08-05',daysRemaining:365,status:'CURRENT'},operationsManual:null,warnings:{renewalsOverdue:1,legalHolds:0,missingEvidence:2},personnel:{missingCertificates:1,unverifiedCertificates:2},aircraft:{missingEvidence:1},training:{outstanding:3},checklists:{missing:1},healthScore:{modelVersion:'AU-CASA-HEALTH-1',evaluationTimestamp:'2026-08-05T00:00:00Z',percentage:92,status:'CRITICAL',criticalBlockers:[{criticalRuleCode:'REOC_EVIDENCE_MISSING',criticalRuleVersion:1,reason:'ReOC evidence is missing.',sourceEntityType:'organisation_compliance_instrument',sourceEntityId:'11111111-1111-4111-8111-111111111111',sourceRowVersion:2,affectedArea:'ReOC',evaluationTimestamp:'2026-08-05T00:00:00Z',route:'/compliance'}],categories:[{code:'REOC',label:'ReOC and organisation certificates',earnedPoints:0,weight:20,counts:{missing:1},sources:[{state:'MISSING',reason:'ReOC evidence is missing.',sourceEntityType:'organisation_compliance_instrument',sourceEntityId:'11111111-1111-4111-8111-111111111111',sourceRowVersion:2,route:'/compliance'}]}]},calendar:{events:[{eventKey:'REOC:1:EXPIRY',title:'ReOC expiry',recordType:'REOC',dueDate:'2026-08-20',daysRemaining:15,state:'DUE_30',route:'/compliance'}],facets:{recordTypes:['REOC']}}}),saveInstrument:jest.fn(),publishManual:jest.fn()})}));
test('shows the explicit CASA command-centre domains and states',async()=>{render(<CasaComplianceOverview/>);expect(await screen.findByText('CASA.REOC.123')).toBeInTheDocument();expect(screen.getByText('CASA Compliance Overview')).toBeInTheDocument();for(const label of['ReOC','Operations Manual','Personnel Credentials','Aircraft Approvals','Training','Checklists','Missing Evidence','Renewals','Retention & Legal Hold'])expect(screen.getByText(label)).toBeInTheDocument();expect(screen.getAllByText(/Missing|Under review|Current|Expired|Due/i).length).toBeGreaterThan(2);});

test('explains compliance health and provides calendar views',async()=>{render(<CasaComplianceOverview/>);expect(await screen.findByText(/92%/)).toBeInTheDocument();expect(screen.getByText(/based on records currently held/i)).toBeInTheDocument();expect(screen.getAllByText('ReOC evidence is missing.').length).toBeGreaterThan(1);expect(screen.getByText('AU-CASA-HEALTH-1')).toBeInTheDocument();expect(screen.getAllByText(/Evaluated 5 Aug 2026/i).length).toBeGreaterThan(0);expect(screen.getAllByText(/organisation_compliance_instrument · 11111111/i).length).toBeGreaterThan(1);expect(screen.getAllByText(/Source row version 2/i).length).toBeGreaterThan(1);for(const view of['Next 90 days','Agenda','Month','Overdue','Due soon','Completed'])expect(screen.getByRole('button',{name:view})).toBeInTheDocument();expect(screen.getByText('ReOC expiry')).toBeInTheDocument();});
