import React from'react';import{Alert,Box,Button,Card,CardContent,Checkbox,FormControlLabel,Grid,MenuItem,Stack,TextField,Typography}from'@mui/material';import{createChecklistsApi}from'../services/checklistsApi';
const categories=['PRE_FLIGHT','POST_FLIGHT','AIRCRAFT_SETUP','EQUIPMENT_SETUP','BATTERY_HANDLING','CHEMICAL_LOADING','TANK_MIXING','SITE_SETUP','WEATHER','EMERGENCY_RESPONSE','MAINTENANCE','VEHICLE_TRAILER','WASHDOWN','NIGHT_OPERATIONS','SPECIAL_OPERATIONS','CUSTOM'];const types=['YES_NO','PASS_FAIL','COMPLETE_INCOMPLETE','NUMERIC','TEXT','DATETIME','PERSONNEL','AIRCRAFT','EQUIPMENT','PHOTO','FILE','SIGNATURE','ACKNOWLEDGEMENT','CUSTOM_OPTIONS'];
export default function ControlledChecklists(){const api=React.useMemo(()=>createChecklistsApi(),[]),[templates,setTemplates]=React.useState<any[]>([]),[open,setOpen]=React.useState(false),[message,setMessage]=React.useState(''),[name,setName]=React.useState(''),[category,setCategory]=React.useState('PRE_FLIGHT'),[stage,setStage]=React.useState('PRE_FLIGHT'),[prompt,setPrompt]=React.useState(''),[itemType,setItemType]=React.useState('PASS_FAIL'),[items,setItems]=React.useState<any[]>([]),[failure,setFailure]=React.useState({comment:false,photo:false,file:false,action:false,signoff:false,repeat:false});const load=React.useCallback(()=>api.templates().then(setTemplates).catch(e=>setMessage(e.message)),[api]);React.useEffect(()=>{void load()},[load]);const add=()=>{if(!prompt.trim())return;setItems(current=>[...current,{id:`item-${current.length+1}`,prompt:prompt.trim(),type:itemType,mandatory:true,required_comment_on_failure:failure.comment,required_photo_on_failure:failure.photo,required_file_on_failure:failure.file,create_corrective_action_on_failure:failure.action,requires_signoff_on_failure:failure.signoff,repeat_required_on_failure:failure.repeat}]);setPrompt('');};const publish=async()=>{try{let list=items;if(prompt.trim()){list=[...items,{id:`item-${items.length+1}`,prompt:prompt.trim(),type:itemType,mandatory:true,required_comment_on_failure:failure.comment,required_photo_on_failure:failure.photo,required_file_on_failure:failure.file,create_corrective_action_on_failure:failure.action,requires_signoff_on_failure:failure.signoff,repeat_required_on_failure:failure.repeat}];setItems(list)}if(!name.trim()||!list.length)return;const created=await api.createTemplate({stableCode:`CUSTOM_${Date.now()}`,name:name.trim(),category,applicableLifecycleStages:[stage]});const record=created.record||created;await api.publish({templateId:record.id,expectedVersion:record.row_version||record.rowVersion||1,changeSummary:'Initial controlled version',effectiveAt:new Date().toISOString(),sections:[{id:'section-1',title:'Checks',items:list}],visibilityRules:[],signoffPolicy:{mode:'COMPLETING_PERSONNEL'}});setMessage('Controlled checklist version published.');setOpen(false);await load();}catch(e){setMessage(e instanceof Error?e.message:'Checklist could not be published.')}};return<Box sx={{maxWidth:1200,mx:'auto'}}><Stack direction={{xs:'column',sm:'row'}} justifyContent="space-between" gap={2} sx={{mb:3}}><Box><Typography variant="h3" fontWeight={800}>Checklists</Typography><Typography color="text.secondary">Organisation-controlled operational checklists. Published versions are immutable.</Typography></Box><Button variant="contained" onClick={()=>setOpen(true)}>Create checklist</Button></Stack>{message&&<Alert severity={message.includes('published')?'success':'error'} sx={{mb:2}}>{message}</Alert>}{open&&<Card variant="outlined" sx={{mb:3}}><CardContent><Grid container spacing={2}><Grid size={{xs:12,md:6}}><TextField required fullWidth label="Checklist name" inputProps={{'aria-label':'Checklist name'}} value={name} onChange={e=>setName(e.target.value)}/></Grid><Grid size={{xs:12,md:3}}><TextField select fullWidth label="Category" value={category} onChange={e=>setCategory(e.target.value)}>{categories.map(x=><MenuItem key={x} value={x}>{x.replaceAll('_',' ')}</MenuItem>)}</TextField></Grid><Grid size={{xs:12,md:3}}><TextField select fullWidth label="Lifecycle stage" value={stage} onChange={e=>setStage(e.target.value)}>{['PLANNING','PRE_FLIGHT','OPERATIONAL','COMPLETION','POST_MISSION'].map(x=><MenuItem key={x} value={x}>{x.replaceAll('_',' ')}</MenuItem>)}</TextField></Grid><Grid size={{xs:12,md:8}}><TextField fullWidth label="Item prompt" inputProps={{'aria-label':'Item prompt'}} value={prompt} onChange={e=>setPrompt(e.target.value)}/></Grid><Grid size={{xs:12,md:4}}><TextField select fullWidth label="Response type" value={itemType} onChange={e=>setItemType(e.target.value)}>{types.map(x=><MenuItem key={x} value={x}>{x.replaceAll('_',' ')}</MenuItem>)}</TextField></Grid><Grid size={{xs:12}}><Stack direction="row" useFlexGap flexWrap="wrap">{([['comment','Comment required on failure'],['photo','Photo required on failure'],['file','File required on failure'],['action','Create corrective action on failure'],['signoff','Sign-off required on failure'],['repeat','Repeat required on failure']]as const).map(([key,label])=><FormControlLabel key={key} control={<Checkbox checked={failure[key]} onChange={(_,checked)=>setFailure(v=>({...v,[key]:checked}))}/>} label={label}/>)}</Stack><Button variant="outlined" onClick={add}>Add item</Button></Grid>{items.map((item,index)=><Grid size={{xs:12}} key={item.id}><Alert severity="info">{index+1}. {item.prompt} · {item.type.replaceAll('_',' ')}</Alert></Grid>)}<Grid size={{xs:12}}><Stack direction="row" spacing={1}><Button variant="contained" disabled={!name.trim()||(!items.length&&!prompt.trim())} onClick={()=>void publish()}>Publish controlled version</Button><Button onClick={()=>setOpen(false)}>Cancel</Button></Stack></Grid></Grid></CardContent></Card>}{templates.length===0?<Alert severity="info">No controlled checklists yet.</Alert>:<Grid container spacing={2}>{templates.map(t=><Grid key={t.id} size={{xs:12,md:6}}><Card variant="outlined"><CardContent><Typography variant="h6" fontWeight={800}>{t.name}</Typography><Typography>{String(t.category).replaceAll('_',' ')} · {t.status}</Typography></CardContent></Card></Grid>)}</Grid>}</Box>}
