const { buildMissionPackViewModel, buildMissionSummaryViewModel } = require('../../server/report-view-models');
const authorised={mission:{snapshot:{mission_number:'PB-001',status:'AUTHORISED'}},businessContext:{client:{name:'Client A'},property:{name:'Property A'},fields:[{name:'Field A'}]},planning:{aircraft:[{snapshot:{registration:'FTF-T100'}}],personnel:{assignments:[{assignment_role:'pilot_in_command',snapshot:{name:'Ben Trollope'}}]}},preflight:{}};
test('Mission Pack selector exposes only its six operational pages and footer identity',()=>{const model=buildMissionPackViewModel({artefact:{version:3},evidence:{missionPackRevision:{pack_snapshot:{evidence:authorised}}}});expect(model.pages.map(x=>x.key)).toEqual(['summary','map','weather','chemicals','jsa','preflight']);expect(model.footer).toEqual(expect.objectContaining({missionNumber:'PB-001',client:'Client A',property:'Property A',reportVersion:3}));expect(JSON.stringify(model)).not.toMatch(/provider_key|checksum|customerOutcomes|operationalEvidence/);});
test('legacy completion remains readable but marks unavailable day detail explicitly',()=>{const model=buildMissionSummaryViewModel({evidence:{missionId:'m1',completionRevision:{completion_snapshot:{planningAndPreflightAuthorisation:{evidence_manifest:authorised},operationalEvidence:{actual_start_at:'2026-08-04T06:00:00Z'}}}}});expect(model).toMatchObject({source:'LEGACY_COMPLETION',evidenceStatus:'LEGACY_DETAIL_UNAVAILABLE',operatingDays:[],legacyActual:{start:'2026-08-04T06:00:00Z'}});expect(model.identity.missionNumber).toBe('PB-001');});
