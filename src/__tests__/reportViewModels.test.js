const{buildMissionPackViewModel,buildMissionSummaryViewModel}=require('../../server/report-view-models');const authorised={mission:{snapshot:{mission_number:'PB-001',status:'AUTHORISED'}},businessContext:{client:{name:'Client A'},property:{name:'Property A'},fields:[{name:'Field A'}]},planning:{aircraft:[{snapshot:{registration:'FTF-T100'}}],personnel:{assignments:[{assignment_role:'pilot_in_command',snapshot:{name:'Ben Trollope'}}]}},preflight:{}};test('Mission Pack selector exposes only its six operational pages and footer identity',()=>{const model=buildMissionPackViewModel({artefact:{version:3},evidence:{missionPackRevision:{pack_snapshot:{evidence:authorised}}}});expect(model.pages.map(x=>x.key)).toEqual(['summary','map','weather','chemicals','jsa','preflight']);expect(model.footer).toEqual(expect.objectContaining({missionNumber:'PB-001',client:'Client A',property:'Property A',reportVersion:3}));expect(JSON.stringify(model)).not.toMatch(/provider_key|checksum|customerOutcomes|operationalEvidence/);});test('Mission Summary completion status is derived from immutable exceptions',()=>{const normal=buildMissionSummaryViewModel({evidence:{missionId:'m1',completionRevision:{completion_snapshot:{planningAndPreflightAuthorisation:{evidence_manifest:authorised}}}}}),exception=buildMissionSummaryViewModel({evidence:{missionId:'m1',completionRevision:{flight_lines_override:true,completion_snapshot:{planningAndPreflightAuthorisation:{evidence_manifest:authorised}}}}});expect(normal.completionStatus).toBe('Mission completed successfully.');expect(exception.completionStatus).toBe('Mission completed with operational exceptions.');});
test('Mission Summary selects actual operations from the repository evidence shape',()=>{const model=buildMissionSummaryViewModel({evidence:{missionId:'m1',completionRevision:{completed_at:'2026-08-04T08:00:00Z',completion_snapshot:{planningAndPreflightAuthorisation:{evidence_manifest:authorised}}},operationalRevision:{actual_start_at:'2026-08-04T06:00:00Z'},actualResources:{actual_resources:{aircraft:[{registration:'FTF-T100'}],personnel:[{name:'Ben Trollope'}]}},actualChemicals:{actual_usage:{actual_water_litres:850,lines:[{product_name:'Product A'}]}},imports:[{original_filename:'final-flight.kml',derived_statistics:{area_hectares:12.4,coverage_percent:98.7},flight_lines:{type:'MultiLineString'}}]}});expect(model.actual.water).toBe(850);expect(model.actual.area).toBe(12.4);expect(model.actual.aircraft).toEqual([{registration:'FTF-T100'}]);expect(model.coverage).toEqual(expect.objectContaining({finalKml:'final-flight.kml',flightLines:{type:'MultiLineString'}}));});
