const fs=require('fs'),path=require('path'),sql=fs.readFileSync(path.resolve(__dirname,'../../supabase/migrations/20260805143000_mission_checklist_readiness_integration.sql'),'utf8');test('mission authorisation readiness selects immutable checklist evidence and precise blockers',()=>{expect(sql).toContain('ftf_evaluate_mission_checklist_readiness');expect(sql).toContain("status='SUBMITTED'");expect(sql).toContain("'{preflight,checklists}'");expect(sql).toContain("'{blockers}'");expect(sql).toContain('Controlled Checklists');expect(sql).toContain("to service_role")});
