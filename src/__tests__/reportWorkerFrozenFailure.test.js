const{processNextReportJob}=require('../../server/report-worker');
test('invalid canonical evidence fails permanently without storing or completing a PDF',async()=>{const job={id:'job-1',artefact:{id:'artefact-1',report_type:'MISSION_RECORD',evidence_manifest:{completionRevision:{id:'completion-1'}}}},repository={claim:jest.fn().mockResolvedValue(job),loadFrozenReportDocument:jest.fn().mockResolvedValue({status:'AVAILABLE',documentText:'{}',documentDigest:'0'.repeat(64)}),store:jest.fn(),complete:jest.fn(),fail:jest.fn()},logger={error:jest.fn()};expect(await processNextReportJob({workerId:'worker',repository,logger})).toMatchObject({processed:false,failed:true});expect(repository.store).not.toHaveBeenCalled();expect(repository.complete).not.toHaveBeenCalled();expect(repository.fail).toHaveBeenCalledWith('job-1','artefact-1','FROZEN_REPORT_EVIDENCE_INVALID','Frozen mission report evidence is unavailable or invalid.');expect(logger.error).toHaveBeenCalledWith('report_generation_failed',expect.objectContaining({message:'Frozen mission report evidence is unavailable or invalid.'}));});
