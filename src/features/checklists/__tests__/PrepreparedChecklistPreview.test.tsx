import{render,screen}from'@testing-library/react';import userEvent from'@testing-library/user-event';import{PrepreparedChecklistPreview}from'../PrepreparedChecklistPreview';import{composeDevelopmentChecklist,developmentProfiles}from'../prepreparedChecklistEngine';
const context={aircraft:{id:'aircraft-1',manufacturer:'DJI',model:'T100'},mission:{id:'mission-1'},operatingLocation:{id:'base-1'},fleetReady:true,configuration:'SPRAY' as const,rtk:'ENABLED' as const,compassCalibrationRequired:false,flowCalibrationRequired:false};
test('presents resolved facts separately and one progressive operator section at a time',async()=>{render(<PrepreparedChecklistPreview result={composeDevelopmentChecklist(developmentProfiles.T100_PRE_FLIGHT,context)}/>);expect(screen.getByText('19 operator actions')).toBeVisible();expect(screen.getByText('6 facts already resolved')).toBeVisible();expect(screen.getByRole('button',{name:/Aircraft and propulsion/})).toHaveAttribute('aria-expanded','true');expect(screen.queryByText('Assess current weather and wind')).not.toBeVisible();await userEvent.click(screen.getByRole('button',{name:/Mission and site/}));expect(screen.getByText('Assess current weather and wind')).toBeVisible();});
test('blocks completion when applicability needs review',()=>{render(<PrepreparedChecklistPreview result={composeDevelopmentChecklist(developmentProfiles.T100_PRE_FLIGHT,{...context,rtk:'UNKNOWN'})}/>);expect(screen.getByText(/Review required:/)).toBeVisible();expect(screen.getByRole('button',{name:'Begin checklist'})).toBeDisabled();});
