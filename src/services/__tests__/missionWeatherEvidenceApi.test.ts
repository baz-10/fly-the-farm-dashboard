import{createMissionWeatherEvidenceApi}from'../missionWeatherEvidenceApi';test('uses only authoritative Mission Weather routes',async()=>{const f=jest.fn().mockResolvedValue({ok:true,status:200,json:async()=>({data:[]})})as any;const api=createMissionWeatherEvidenceApi(f);await api.read('eeeeeeee-eeee-4eee-8eee-eeeeeeeeeeee');expect(f).toHaveBeenCalledWith('/api/v1/mission-weather?missionId=eeeeeeee-eeee-4eee-8eee-eeeeeeeeeeee',expect.objectContaining({credentials:'same-origin'}));});
test('uses distinct Planning forecast and Pre-flight observation commands',async()=>{const f=jest.fn().mockResolvedValue({ok:true,status:200,json:async()=>({data:[]})})as any,api=createMissionWeatherEvidenceApi(f),mission='eeeeeeee-eeee-4eee-8eee-eeeeeeeeeeee';await api.forecasts(mission);await api.createForecast(mission,{expectedVersion:0,latitude:-27,longitude:153,validFrom:'2026-08-10T08:00:00Z',validTo:'2026-08-10T12:00:00Z'});await api.selectForecast(mission,'11111111-1111-4111-8111-111111111111',0);expect(f.mock.calls.map((call:any[])=>call[0])).toEqual([`/api/v1/mission-weather?missionId=${mission}&action=forecast`,`/api/v1/mission-weather?missionId=${mission}&action=forecast`,`/api/v1/mission-weather?missionId=${mission}&action=select-forecast`]);expect(JSON.parse(f.mock.calls[1][1].body)).toEqual(expect.objectContaining({provider:'OPEN_METEO'}));});
