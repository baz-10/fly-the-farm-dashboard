type Fetcher=typeof fetch;async function request(f:Fetcher,path:string,init:RequestInit={}){const r=await f(path,{credentials:'same-origin',headers:{'Content-Type':'application/json'},...init}),p=await r.json();if(!r.ok)throw Object.assign(new Error(p.error?.message||'Mission Weather request failed.'),{code:p.error?.code,meta:p.error?.meta});return p.data;}export const createMissionWeatherEvidenceApi=(f:Fetcher=fetch)=>({read:(missionId:string)=>request(f,`/api/v1/mission-weather?missionId=${encodeURIComponent(missionId)}`),forecasts:(missionId:string)=>request(f,`/api/v1/mission-weather?missionId=${encodeURIComponent(missionId)}&action=forecast`),readiness:(missionId:string)=>request(f,`/api/v1/mission-weather?missionId=${encodeURIComponent(missionId)}&action=readiness`),createForecast:(missionId:string,input:Record<string,unknown>)=>request(f,`/api/v1/mission-weather?missionId=${encodeURIComponent(missionId)}&action=forecast`,{method:'POST',body:JSON.stringify({...input,provider:'OPEN_METEO'})}),selectForecast:(missionId:string,forecastRevisionId:string,expectedVersion:number)=>request(f,`/api/v1/mission-weather?missionId=${encodeURIComponent(missionId)}&action=select-forecast`,{method:'POST',body:JSON.stringify({forecastRevisionId,expectedVersion})}),createManual:(missionId:string,input:Record<string,unknown>)=>request(f,`/api/v1/mission-weather?missionId=${encodeURIComponent(missionId)}`,{method:'POST',body:JSON.stringify({...input,source:'MANUAL'})}),select:(missionId:string,observationId:string,expectedVersion:number)=>request(f,`/api/v1/mission-weather?missionId=${encodeURIComponent(missionId)}&action=select`,{method:'POST',body:JSON.stringify({observationId,expectedVersion})})});
