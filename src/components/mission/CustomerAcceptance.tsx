import React from'react';
import{Alert,Box,Button,Card,CardContent,Chip,MenuItem,Stack,TextField,Typography}from'@mui/material';
import{customerAcceptanceApi}from'../../services/customerAcceptanceApi';
const now=()=>new Date().toISOString().slice(0,16);
export default function CustomerAcceptance({missionId,api=customerAcceptanceApi}:{missionId:string;api?:any}){const[state,setState]=React.useState<any>(null),[mode,setMode]=React.useState<'idle'|'record'>('idle'),[error,setError]=React.useState(''),[saving,setSaving]=React.useState(false),[link,setLink]=React.useState('');const[form,setForm]=React.useState<any>({stateCode:'ACCEPTED',methodCode:'PHONE',customerContactName:'',customerContactRole:'',customerContactEmail:'',customerContactPhone:'',acknowledgedAt:now(),comments:'',supersedesAcceptanceId:''});const load=React.useCallback(()=>api.read(missionId).then((x:any)=>{setState(x);setForm((f:any)=>({...f,customerContactName:f.customerContactName||x.context?.customer?.contactName||x.context?.customer?.name||'',customerContactEmail:f.customerContactEmail||x.context?.customer?.contactEmail||'',customerContactPhone:f.customerContactPhone||x.context?.customer?.contactPhone||''}));}).catch((e:any)=>setError(e.message)),[api,missionId]);React.useEffect(()=>{load();},[load]);const set=(k:string)=>(e:React.ChangeEvent<HTMLInputElement>)=>setForm((f:any)=>({...f,[k]:e.target.value}));const save=async()=>{setSaving(true);setError('');try{await api.record(missionId,{...form,acknowledgedAt:new Date(form.acknowledgedAt).toISOString(),supersedesAcceptanceId:form.supersedesAcceptanceId||null});setMode('idle');await load();}catch(e:any){setError(e.message);}finally{setSaving(false);}};const issue=async()=>{setSaving(true);setError('');try{const result=await api.issueLink(missionId,{contactName:form.customerContactName||state?.context?.customer?.contactName||state?.context?.customer?.name,contactEmail:form.customerContactEmail||state?.context?.customer?.contactEmail,expiresInHours:48});setLink(`${window.location.origin}/customer-acceptance/${result.token}`);await load();}catch(e:any){setError(e.message);}finally{setSaving(false);}};if(!state&&!error)return<Card><CardContent><Typography>Loading Customer Acceptance…</Typography></CardContent></Card>;return<Card><CardContent><Stack spacing={2}><Box><Typography variant="h5">Customer Acceptance</Typography><Typography color="text.secondary">Optional immutable acknowledgement of completed work. Mission Completion and Mission Outcomes remain unchanged.</Typography></Box>{error&&<Alert severity="error">{error}</Alert>}{!state?.context&&<Alert severity="info">Complete the Mission before recording customer acceptance.</Alert>}{(state?.records||[]).length===0&&state?.context&&<Alert severity="info">No customer acknowledgement has been recorded. This does not block Mission Completion.</Alert>}{(state?.records||[]).map((r:any)=><Box key={r.id} sx={{borderLeft:'4px solid',borderColor:r.state_snapshot?.code==='DISPUTED'?'warning.main':'primary.main',pl:2,py:1}}><Stack direction="row" spacing={1} flexWrap="wrap"><Typography fontWeight={700}>{r.state_snapshot?.display_name||r.state_snapshot?.code}</Typography><Chip size="small" label={r.method_snapshot?.display_name||r.submission_channel}/></Stack><Typography>{r.customer_contact_name} · {new Date(r.acknowledged_at).toLocaleString()}</Typography>{r.customer_comments&&<Typography color="text.secondary">{r.customer_comments}</Typography>}{r.supersedes_acceptance_id&&<Alert severity="warning">Correction supersedes acceptance {r.supersedes_acceptance_id}</Alert>}</Box>)}{link&&<Alert severity="success"><Typography fontWeight={700}>Secure link created</Typography><TextField fullWidth value={link} InputProps={{readOnly:true}} sx={{mt:1}}/><Button onClick={()=>navigator.clipboard?.writeText(link)}>Copy link</Button></Alert>}{mode==='idle'&&state?.context&&<Stack direction={{xs:'column',sm:'row'}} spacing={1}><Button variant="contained" onClick={()=>setMode('record')}>Record customer acknowledgement</Button><Button variant="outlined" disabled={saving} onClick={issue}>Create secure customer link</Button></Stack>}{mode==='record'&&<Stack spacing={2}><Typography variant="h6">Record acknowledgement received by Fly The Farm</Typography><Stack direction={{xs:'column',md:'row'}} spacing={2}><TextField fullWidth select label="Acceptance" value={form.stateCode} onChange={set('stateCode')}>{(state?.catalogues?.states||[]).map((x:any)=><MenuItem key={x.code} value={x.code}>{x.display_name}</MenuItem>)}</TextField><TextField fullWidth select label="Acknowledgement method" value={form.methodCode} onChange={set('methodCode')}>{(state?.catalogues?.methods||[]).filter((x:any)=>!['SECURE_LINK','PORTAL'].includes(x.code)).map((x:any)=><MenuItem key={x.code} value={x.code}>{x.display_name}</MenuItem>)}</TextField></Stack><TextField required fullWidth label="Customer contact name" value={form.customerContactName} onChange={set('customerContactName')}/><Stack direction={{xs:'column',md:'row'}} spacing={2}><TextField fullWidth label="Contact role" value={form.customerContactRole} onChange={set('customerContactRole')}/><TextField fullWidth label="Email" value={form.customerContactEmail} onChange={set('customerContactEmail')}/><TextField fullWidth label="Phone" value={form.customerContactPhone} onChange={set('customerContactPhone')}/></Stack><TextField required type="datetime-local" InputLabelProps={{shrink:true}} label="Acknowledgement time" value={form.acknowledgedAt} onChange={set('acknowledgedAt')}/><TextField required fullWidth multiline minRows={3} label="Comments / evidence notes" value={form.comments} onChange={set('comments')}/>{(state?.records||[]).length>0&&<TextField fullWidth select label="Correction of previous acknowledgement (optional)" value={form.supersedesAcceptanceId} onChange={set('supersedesAcceptanceId')}><MenuItem value="">Not a correction</MenuItem>{state.records.map((r:any)=><MenuItem key={r.id} value={r.id}>Acceptance {r.sequence_number}</MenuItem>)}</TextField>}<Stack direction="row" spacing={1}><Button variant="contained" disabled={saving||!form.customerContactName.trim()||!form.comments.trim()} onClick={save}>Save immutable acknowledgement</Button><Button onClick={()=>setMode('idle')}>Cancel</Button></Stack></Stack>}</Stack></CardContent></Card>}
