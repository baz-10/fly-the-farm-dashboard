import{render,screen}from'@testing-library/react';import MissionRecord from'../MissionRecord';jest.mock('../../reports/ReportArtefactStatus',()=>({__esModule:true,default:({reportType}:any)=><div>{reportType}</div>}));test('explains immutable post-completion history and exposes Mission Record generation',()=>{render(<MissionRecord missionId="m1"/>);expect(screen.getByText('Mission Record')).toBeInTheDocument();expect(screen.getByText(/permanent historical report/)).toBeInTheDocument();expect(screen.getByText('MISSION_RECORD')).toBeInTheDocument();});
