import React from 'react';
import {fireEvent,render,screen,waitFor}from '@testing-library/react';
import CustomerAcceptance from '../CustomerAcceptance';

const state={context:{completionRevisionId:'c',customer:{name:'Acme',contactName:'Jane',contactEmail:'jane@example.com'}},records:[],links:[],catalogues:{states:[{code:'ACCEPTED',display_name:'Accepted'}],methods:[{code:'PHONE',display_name:'Phone'}]}};
test('records optional immutable operator acknowledgement and does not offer mutation',async()=>{const api={read:jest.fn().mockResolvedValue(state),record:jest.fn().mockResolvedValue({id:'a'}),issueLink:jest.fn(),revokeLink:jest.fn()};render(<CustomerAcceptance missionId="m" api={api}/>);expect(await screen.findByText('Customer Acceptance')).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:'Record customer acknowledgement'}));fireEvent.change(screen.getByLabelText(/Comments \/ evidence notes/),{target:{value:'Accepted by telephone.'}});fireEvent.click(screen.getByRole('button',{name:'Save immutable acknowledgement'}));await waitFor(()=>expect(api.record).toHaveBeenCalled());expect(screen.queryByRole('button',{name:/edit acceptance/i})).not.toBeInTheDocument();});

test('issues a secure customer link explicitly',async()=>{const api={read:jest.fn().mockResolvedValue(state),record:jest.fn(),issueLink:jest.fn().mockResolvedValue({token:'secret'}),revokeLink:jest.fn()};render(<CustomerAcceptance missionId="m" api={api}/>);await screen.findByText('Customer Acceptance');fireEvent.click(screen.getByRole('button',{name:'Create secure customer link'}));await waitFor(()=>expect(api.issueLink).toHaveBeenCalledWith('m',expect.objectContaining({contactName:'Jane'})));expect(await screen.findByDisplayValue(/customer-acceptance\/secret/)).toBeInTheDocument();});
