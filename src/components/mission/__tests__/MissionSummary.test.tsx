import{render,screen}from'@testing-library/react';import MissionSummary from'../MissionSummary';jest.mock('../../reports/ReportArtefactStatus',()=>({__esModule:true,default:({reportType}:any)=><div>{reportType}</div>}));test('presents the frozen final-signoff Mission Summary contract',()=>{render(<MissionSummary missionId="m1"/>);expect(screen.getByRole('heading',{name:'Mission Summary'})).toBeInTheDocument();expect(screen.getByText(/frozen final-signoff evidence/i)).toBeInTheDocument();expect(screen.getByText(/missing historical detail is labelled unavailable/i)).toBeInTheDocument();expect(screen.getByText('MISSION_SUMMARY')).toBeInTheDocument();});
