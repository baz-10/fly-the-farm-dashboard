import React from 'react';import{fireEvent,render,screen,waitFor}from'@testing-library/react';import AuthoritativeMissionJsa from '../AuthoritativeMissionJsa';
const questions=['maps','weather','notam','people','rights-of-way','landing-areas','public-separation','obstructions','interference','vlos','pilot-ability','privacy','signage'];
const api={read:jest.fn().mockResolvedValue(null),readiness:jest.fn().mockResolvedValue({overallState:'NOT_STARTED',ready:false,blockers:[{code:'JSA_NOT_STARTED',category:'Questions',message:'Complete the Mission JSA.'}],warnings:[],categories:{Questions:'OUTSTANDING'},outstandingSections:['Questions'],completedSections:[]}),save:jest.fn().mockResolvedValue({id:'55555555-5555-4555-8555-555555555555',version_number:1,responses:[],controls:[],hazards:[],attachments:[],approvals:[],template_version:1,policy_version:1}),approve:jest.fn()};
const personnelApi={list:jest.fn().mockResolvedValue([{id:'66666666-6666-4666-8666-666666666666',fullName:'Alex Pilot',isActive:true}])};
test('preserves all Mission Checks and saves only through authoritative API',async()=>{render(<AuthoritativeMissionJsa open missionId="44444444-4444-4444-8444-444444444444" missionName="Real Mission" operatingLocationId="33333333-3333-4333-8333-333333333333" api={api as any} personnelApi={personnelApi as any} onClose={()=>{}}/>);expect(await screen.findByText('Mission Checks')).toBeInTheDocument();expect(screen.getAllByRole('radiogroup')).toHaveLength(13);expect(screen.queryByLabelText(/pilot name/i)).not.toBeInTheDocument();const save=screen.getByRole('button',{name:/save draft/i});await waitFor(()=>expect(save).toBeEnabled());fireEvent.click(save);await waitFor(()=>expect(api.save).toHaveBeenCalled());expect(api.save.mock.calls[0][1].responses.map((r:any)=>r.questionId)).toEqual(questions);expect(JSON.stringify(api.save.mock.calls[0][1])).not.toMatch(/localStorage|sessionStorage/);});
