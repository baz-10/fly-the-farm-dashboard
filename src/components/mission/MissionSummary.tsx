import React from'react';import{Alert,Stack,Typography}from'@mui/material';import ReportArtefactStatus from'../reports/ReportArtefactStatus';export default function MissionSummary({missionId}:{missionId:string}){return<Stack spacing={1.5}><Typography variant="h6">Mission Summary</Typography><Alert severity="info">This summary is rendered from frozen final-signoff evidence. Missing historical detail is labelled unavailable and is never replaced with current Mission data.</Alert><ReportArtefactStatus missionId={missionId} reportType="MISSION_SUMMARY"/></Stack>}
