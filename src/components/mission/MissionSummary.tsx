import React from'react';import{Alert,Stack,Typography}from'@mui/material';import ReportArtefactStatus from'../reports/ReportArtefactStatus';import { WorkflowMaturityBoundary } from '../../components/productMaturity/WorkflowMaturityBoundary';export default function MissionSummary({missionId}:{missionId:string}){return<Stack spacing={1.5}><Typography variant="h6">Mission Summary</Typography><Alert severity="info">A concise two-page summary of what actually happened, including operational actuals, coverage and Customer Outcome. New report versions never replace earlier summaries.</Alert><WorkflowMaturityBoundary moduleCode="mission-workspace" workflowCode="reports"><ReportArtefactStatus missionId={missionId} reportType="MISSION_SUMMARY"/></WorkflowMaturityBoundary></Stack>}
