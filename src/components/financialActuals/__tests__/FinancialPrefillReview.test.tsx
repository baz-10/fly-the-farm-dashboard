import React from'react';import{render,screen}from'@testing-library/react';import userEvent from'@testing-library/user-event';import{FinancialPrefillReview}from'../FinancialPrefillReview';
const ID='10000000-0000-4000-8000-000000000001',prefill:any={proposalDigest:'a'.repeat(64),facts:[{evidenceIdentity:'evidence-1',fieldPath:'revenue/actualHectares',value:'146.200000',unitCode:'HECTARE',sourceVersion:'2'}]};
test('fails closed until an explicit accept/retain/override decision is reviewed',async()=>{const submit=jest.fn();render(<FinancialPrefillReview prefill={prefill} busy={false} onSubmit={submit}/>);const button=screen.getByRole('button',{name:'Apply reviewed selections'});expect(button).toBeDisabled();await userEvent.click(screen.getByLabelText('Review decision'));await userEvent.click(screen.getByRole('option',{name:'Override source value'}));expect(button).toBeDisabled();await userEvent.type(screen.getByLabelText('Effective value'),'144.800000');await userEvent.type(screen.getByLabelText('Override reason'),'Verified field log');await userEvent.click(button);expect(submit).toHaveBeenCalledWith({proposalDigest:'a'.repeat(64),selections:[{evidenceIdentity:'evidence-1',action:'OVERRIDE',effectiveValue:'144.800000',overrideReason:'Verified field log'}]})});
