import React,{useState}from'react';import{Button,Card,CardContent,MenuItem,Stack,TextField,Typography}from'@mui/material';import type{FinancialPrefill}from'../../types/financialActuals';
type Choice={action:'ACCEPT'|'RETAIN'|'OVERRIDE';effectiveValue?:string;overrideReason?:string};
export function FinancialPrefillReview({prefill,busy,onSubmit}:{prefill:FinancialPrefill;busy:boolean;onSubmit:(payload:Record<string,unknown>)=>void}){const[choices,setChoices]=useState<Record<string,Choice>>(()=>Object.fromEntries(prefill.facts.map(f=>[f.evidenceIdentity,{action:'RETAIN'}]))),set=(id:string,value:Partial<Choice>)=>setChoices(previous=>({...previous,[id]:{...previous[id],...value}}));const selections=prefill.facts.map(f=>({evidenceIdentity:f.evidenceIdentity,...choices[f.evidenceIdentity]})),reviewed=selections.some(x=>x.action!=='RETAIN'),valid=reviewed&&selections.every(x=>x.action!=='OVERRIDE'||Boolean(x.effectiveValue?.trim()&&x.overrideReason?.trim()));return <Card variant="outlined" sx={{my:2}}><CardContent><Typography variant="h6">Operational prefill</Typography><Typography color="text.secondary">Choose Accept, Retain current Draft, or Override with a reason.</Typography><Stack spacing={2} mt={2}>{prefill.facts.map(f=>{const choice=choices[f.evidenceIdentity];return <Card variant="outlined" key={f.evidenceIdentity}><CardContent><Typography fontWeight={700}>{f.fieldPath}</Typography><Typography>{f.value} {f.unitCode}</Typography><Typography variant="caption">Source: Mission closeout · version {f.sourceVersion}</Typography><TextField select fullWidth label="Review decision" value={choice.action} onChange={e=>set(f.evidenceIdentity,{action:e.target.value as Choice['action']})} sx={{mt:1}}><MenuItem value="RETAIN">Retain current Draft</MenuItem><MenuItem value="ACCEPT">Accept source value</MenuItem>{f.unitCode!=='IDENTITY'&&<MenuItem value="OVERRIDE">Override source value</MenuItem>}</TextField>{choice.action==='OVERRIDE'&&<Stack spacing={1} mt={1}><TextField label="Effective value" value={choice.effectiveValue||''} onChange={e=>set(f.evidenceIdentity,{effectiveValue:e.target.value})}/><TextField label="Override reason" value={choice.overrideReason||''} onChange={e=>set(f.evidenceIdentity,{overrideReason:e.target.value})}/></Stack>}</CardContent></Card>})}</Stack><Button variant="contained" disabled={busy||!valid} onClick={()=>onSubmit({proposalDigest:prefill.proposalDigest,selections})} sx={{mt:2}}>Apply reviewed selections</Button></CardContent></Card>}
