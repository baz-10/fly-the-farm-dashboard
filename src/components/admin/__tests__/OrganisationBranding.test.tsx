import{render,screen,fireEvent,waitFor}from'@testing-library/react';import OrganisationBranding from'../OrganisationBranding';
const state={organisation:{profile:{row_version:1,report_display_name:'Fly The Farm',legal_business_name:'Fly The Farm Pty Ltd',primary_email:'ops@example.com'},entitlement:{capabilityCode:'ORGANISATION_BRANDING'},attributionText:'Generated by Spray Command'},logos:[]};
test('shows the exact report identity preview and mandatory attribution',async()=>{const api={read:jest.fn().mockResolvedValue(state),update:jest.fn().mockResolvedValue(state),uploadLogo:jest.fn(),activateLogo:jest.fn(),removeLogo:jest.fn()};render(<OrganisationBranding api={api as any}/>);expect(await screen.findByText('Report identity')).toBeInTheDocument();expect(screen.getByText('Generated by Spray Command')).toBeInTheDocument();expect(screen.getByDisplayValue('Fly The Farm')).toBeInTheDocument();});
test('saves with optimistic version and disables management without permission',async()=>{const api={read:jest.fn().mockResolvedValue(state),update:jest.fn().mockResolvedValue(state),uploadLogo:jest.fn(),activateLogo:jest.fn(),removeLogo:jest.fn()};const{rerender}=render(<OrganisationBranding api={api as any}/>);fireEvent.click(await screen.findByRole('button',{name:'Save report identity'}));await waitFor(()=>expect(api.update).toHaveBeenCalledWith(1,expect.objectContaining({reportDisplayName:'Fly The Farm'})));rerender(<OrganisationBranding api={api as any} canManage={false}/>);expect(await screen.findByRole('button',{name:'Save report identity'})).toBeDisabled();});
test('uses one fixed ABN field and persistent cut-out labels for empty inputs',async()=>{const api={read:jest.fn().mockResolvedValue(state),update:jest.fn().mockResolvedValue(state),uploadLogo:jest.fn(),activateLogo:jest.fn(),removeLogo:jest.fn()};render(<OrganisationBranding api={api as any}/>);const abn=await screen.findByLabelText('ABN');expect(screen.queryByLabelText('Business identifier type')).not.toBeInTheDocument();expect(screen.getByLabelText('Trading name')).toHaveAttribute('placeholder',' ');expect(screen.getByLabelText('Website')).toHaveAttribute('placeholder',' ');fireEvent.change(abn,{target:{value:'12 345 678 901'}});fireEvent.click(screen.getByRole('button',{name:'Save report identity'}));await waitFor(()=>expect(api.update).toHaveBeenCalledWith(1,expect.objectContaining({businessIdentifierType:'ABN',businessIdentifierValue:'12 345 678 901'})));});
