import React from'react';import{Alert,Box,Button,Card,CardContent,Chip,Divider,Grid,Stack,TextField,Typography}from'@mui/material';import{organisationBrandingApi}from'../../services/organisationBrandingApi';
const value=(x:any)=>x??'';
export default function OrganisationBranding({api=organisationBrandingApi,canManage=true}:{api?:any;canManage?:boolean}){const[state,setState]=React.useState<any>(null),[form,setForm]=React.useState<any>({}),[error,setError]=React.useState(''),[saving,setSaving]=React.useState(false),[preview,setPreview]=React.useState('');const load=React.useCallback(()=>api.read().then((x:any)=>{setState(x);const p=x.organisation?.profile||x.profile||{};setForm({legalBusinessName:value(p.legal_business_name),tradingName:value(p.trading_name),reportDisplayName:value(p.report_display_name),businessIdentifierType:value(p.business_identifier_type),businessIdentifierValue:value(p.business_identifier_value),primaryPhone:value(p.primary_phone),primaryEmail:value(p.primary_email),website:value(p.website)});}).catch((e:any)=>setError(e.message)),[api]);React.useEffect(()=>{load();},[load]);React.useEffect(()=>()=>{if(preview)URL.revokeObjectURL(preview);},[preview]);const p=state?.organisation?.profile||state?.profile||{},logos=state?.logos||[],entitlement=state?.organisation?.entitlement||state?.entitlement;const set=(k:string)=>(e:any)=>setForm((f:any)=>({...f,[k]:e.target.value}));const run=async(fn:()=>Promise<any>)=>{setSaving(true);setError('');try{await fn();await load();}catch(e:any){setError(e.message);}finally{setSaving(false);}};const upload=async(file?:File)=>{if(!file)return;if(preview)URL.revokeObjectURL(preview);setPreview(URL.createObjectURL(file));await run(()=>api.uploadLogo(file));};if(!state&&!error)return<Typography>Loading organisation branding…</Typography>;return<Card elevation={0} sx={{border:'1px solid',borderColor:'divider'}}><CardContent><Stack spacing={2}><Box><Typography variant="h5">Report identity</Typography><Typography color="text.secondary">Organisation details and logo used by authoritative Spray Command reports.</Typography></Box>{error&&<Alert severity="error">{error}</Alert>}<Alert severity={entitlement?.capabilityCode==='BRANDING_UNAVAILABLE'?'info':'success'}>Capability: {entitlement?.capabilityCode||'Not configured'}. Spray Command attribution remains mandatory.</Alert><Grid container spacing={2}><Grid size={{xs:12,md:6}}><TextField fullWidth label="Legal business name" value={form.legalBusinessName||''} onChange={set('legalBusinessName')}/></Grid><Grid size={{xs:12,md:6}}><TextField fullWidth label="Trading name" value={form.tradingName||''} onChange={set('tradingName')}/></Grid><Grid size={12}><TextField fullWidth label="Report display name" value={form.reportDisplayName||''} onChange={set('reportDisplayName')}/></Grid><Grid size={{xs:12,md:4}}><TextField fullWidth label="Business identifier type" value={form.businessIdentifierType||''} onChange={set('businessIdentifierType')}/></Grid><Grid size={{xs:12,md:8}}><TextField fullWidth label="Business identifier" value={form.businessIdentifierValue||''} onChange={set('businessIdentifierValue')}/></Grid><Grid size={{xs:12,md:6}}><TextField fullWidth label="Primary email" value={form.primaryEmail||''} onChange={set('primaryEmail')}/></Grid><Grid size={{xs:12,md:6}}><TextField fullWidth label="Primary phone" value={form.primaryPhone||''} onChange={set('primaryPhone')}/></Grid><Grid size={12}><TextField fullWidth label="Website" value={form.website||''} onChange={set('website')}/></Grid></Grid><Button variant="contained" disabled={!canManage||saving} onClick={()=>run(()=>api.update(p.row_version,form))}>Save report identity</Button><Divider/><Typography variant="h6">Organisation logo</Typography><Typography variant="body2">PNG, JPEG or WebP · maximum 5 MiB and 6000 × 6000 px. Originals and checksums are retained as immutable versions.</Typography>{preview&&<Box component="img" src={preview} alt="Local logo preview" sx={{maxWidth:320,height:120,objectFit:'contain',alignSelf:'flex-start'}}/>}<Button component="label" variant="outlined" disabled={!canManage||saving}>Upload logo version<input hidden type="file" accept="image/png,image/jpeg,image/webp" onChange={e=>void upload(e.target.files?.[0])}/></Button>{logos.map((logo:any)=><Stack key={`${logo.internalFileId}-${logo.fileVersion}`} direction={{xs:'column',sm:'row'}} spacing={1} alignItems={{sm:'center'}}><Typography sx={{flex:1}}>{logo.originalFilename} · version {logo.fileVersion} · {logo.width}×{logo.height}</Typography><Chip label={logo.active?'Active':'Retained'} color={logo.active?'success':'default'}/>{!logo.active&&<Button disabled={!canManage||saving} onClick={()=>run(()=>api.activateLogo(p.row_version,logo.internalFileId,logo.fileVersion))}>Use this logo</Button>}</Stack>)}{p.active_logo_file_id&&<Button color="warning" disabled={!canManage||saving} onClick={()=>run(()=>api.removeLogo(p.row_version))}>Remove active logo</Button>}<Divider/><Box sx={{border:'1px solid',borderColor:'divider',p:2,borderRadius:2}}><Typography variant="overline">Report header preview</Typography><Typography variant="h5">{form.reportDisplayName||form.tradingName||form.legalBusinessName||'Organisation name'}</Typography><Typography>{form.primaryEmail}{form.primaryPhone?` · ${form.primaryPhone}`:''}</Typography><Typography variant="caption" color="text.secondary">Generated by Spray Command</Typography></Box></Stack></CardContent></Card>}
