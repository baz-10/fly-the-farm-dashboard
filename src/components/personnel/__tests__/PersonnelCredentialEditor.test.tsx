import{render,screen,waitFor}from'@testing-library/react';import userEvent from'@testing-library/user-event';import PersonnelCredentialEditor from'../PersonnelCredentialEditor';
test('records a verified Personnel credential through the authoritative API',async()=>{const api={addCredential:jest.fn().mockResolvedValue({id:'credential-1'})}as any;render(<PersonnelCredentialEditor personnelId="person-1" personnelName="Ben Trollope" api={api} onSaved={jest.fn()}/>);await userEvent.click(screen.getByRole('button',{name:'Add credential'}));for(const[label,value]of[['Credential type','RePL'],['Credential identifier','REAL-ID'],['Issuer','CASA'],['Issue date','2025-01-01'],['Expiry date','2027-01-01']]){const input=screen.getByLabelText(label);await userEvent.clear(input);await userEvent.type(input,value);}await userEvent.click(screen.getByRole('button',{name:'Save verified credential'}));await waitFor(()=>expect(api.addCredential).toHaveBeenCalledWith('person-1',expect.objectContaining({credentialType:'RePL',identifier:'REAL-ID',issuer:'CASA',expiryDate:'2027-01-01',verificationState:'verified'})));expect(await screen.findByText('Credential saved authoritatively.')).toBeInTheDocument();});
