function calculateWetBulbC(t,rh){const h=Math.max(1,Math.min(100,Number(rh)));return t*Math.atan(.151977*Math.sqrt(h+8.313659))+Math.atan(t+h)-Math.atan(h-1.676331)+.00391838*Math.pow(h,1.5)*Math.atan(.023101*h)-4.686035;}
function calculateDeltaT(t,rh){return Math.round((Number(t)-calculateWetBulbC(Number(t),Number(rh)))*10)/10;}
function assessSprayCondition({deltaT,windSpeedKmh,windGustKmh,rainProbability}){const reasons=[];let status='GO';if(deltaT>10){status='NO_GO';reasons.push('Delta T is above the preferred operating range.');}else if(deltaT<2||deltaT>8){status='MARGINAL';reasons.push('Delta T is outside the preferred 2–8°C range.');}if(windSpeedKmh<3||windSpeedKmh>20||windGustKmh>28){status=status==='NO_GO'?'NO_GO':'POOR';reasons.push('Wind or gusts may affect safe application.');}if(rainProbability>=60){status=status==='NO_GO'?'NO_GO':'POOR';reasons.push('Rain risk may affect application.');}return{status,label:status==='GO'?'Good':status==='MARGINAL'?'Marginal':status==='POOR'?'Poor':'No Go',reasons};}
function findBestSprayWindow(hours){let best=null,start=null;for(let i=0;i<=hours.length;i++){const good=i<hours.length&&hours[i]?.sprayCondition?.status==='GO';if(good&&start===null)start=i;if((!good||i===hours.length)&&start!==null){const end=i-1;if(!best||end-start>best.end-best.start)best={start,end};start=null;}}return best?{start:hours[best.start].time,end:hours[best.end].time,status:'GO'}:null;}
function assessInversionPotential({isDay,windSpeedKmh,cloudCoverPercent,humidityPercent,temperatureTrendC=0}){let score=0;if(!isDay)score+=1;if(windSpeedKmh<11)score+=1;if(windSpeedKmh<6)score+=1;if(cloudCoverPercent<30)score+=1;if(humidityPercent>=90)score+=1;if(temperatureTrendC<-.5)score+=1;const rating=score>=4&&!isDay?'high':score>=2?'moderate':'low';return{rating,score:rating==='high'?2:rating==='moderate'?1:0,label:rating==='high'?'High':rating==='moderate'?'Medium':'Low',message:rating==='high'?'Forecast conditions may support a surface inversion. Do not spray until conditions are checked on site.':rating==='moderate'?'An inversion may be possible. Check conditions on site.':'Forecast potential is low. An on-site check is still required.'};}
module.exports={calculateWetBulbC,calculateDeltaT,assessSprayCondition,findBestSprayWindow,assessInversionPotential};
