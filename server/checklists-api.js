const{createHttpError}=require('./supabase');const{resolveRequestContext}=require('./request-context');const{errorEnvelope}=require('./operational-api');const{ChecklistsRepository}=require('./checklists-repository');
const err=(status,code,message)=>Object.assign(createHttpError(status,message),{code});const permitted=(c,p)=>new Set(c.permissions||[]).has('*')||new Set(c.permissions||[]).has(p)||new Set(c.permissions||[]).has('compliance.*');const uuid=(v,n)=>{if(typeof v!=='string'||!/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(v))throw err(400,'VALIDATION_ERROR',`${n} must be a UUID.`);return v};
function createChecklistsHandler(dependencies={}){const repository=dependencies.repository||new ChecklistsRepository(),resolve=dependencies.resolveContext||resolveRequestContext;return async(req,res)=>{res.setHeader('Cache-Control','no-store');res.setHeader('Content-Type','application/json; charset=utf-8');try{const context=await resolve(req,res),action=req.query?.action;if(req.method==='GET'){if(!permitted(context,'compliance.read'))throw err(403,'FORBIDDEN','You do not have permission for this operation.');if(action==='templates')return res.status(200).json({data:await repository.listTemplates(context)});if(action==='mission')return res.status(200).json({data:await repository.readMissionExecutions(context,uuid(req.query?.missionId,'missionId'))});if(action==='readiness')return res.status(200).json({data:await repository.evaluateMissionReadiness(context,uuid(req.query?.missionId,'missionId'),String(req.query?.lifecycleStage||''))});throw err(400,'UNSUPPORTED_ACTION','Unsupported Checklist action.');}if(req.method!=='POST')throw err(405,'METHOD_NOT_ALLOWED','Method not allowed.');const origin=req.headers?.origin;if(origin&&new URL(origin).host!==req.headers?.host)throw err(403,'ORIGIN_FORBIDDEN','Request origin is not permitted.');const b=req.body||{};if(action==='template'||action==='publish'){if(!permitted(context,action==='publish'?'compliance.publish':'compliance.manage'))throw err(403,'FORBIDDEN','You do not have permission for this operation.');const result=action==='publish'?await repository.publishTemplate(context,uuid(b.templateId,'templateId'),Number(b.expectedVersion),b):await repository.writeTemplate(context,b.operation||'CREATE',b.templateId?uuid(b.templateId,'templateId'):null,Number(b.expectedVersion||0),b);return res.status(201).json({data:result});}if(['start','save','submit'].includes(action)){if(!permitted(context,'compliance.manage'))throw err(403,'FORBIDDEN','You do not have permission for this operation.');const operation=action==='start'?'CREATE':action==='save'?'SAVE_DRAFT':'SUBMIT',executionId=b.executionId?uuid(b.executionId,'executionId'):null,result=await repository.writeExecution(context,operation,executionId,Number(b.expectedVersion||0),b);if(result?.conflict)throw err(409,'VERSION_CONFLICT','Checklist execution changed.');if(result?.superseded_checklist_selected)throw err(409,'SUPERSEDED_CHECKLIST_SELECTED','The selected checklist version was superseded before execution.');return res.status(201).json({data:result});}if(action==='evidence'){if(!permitted(context,'compliance.manage'))throw err(403,'FORBIDDEN','You do not have permission for this operation.');return res.status(201).json({data:await repository.stageEvidence(context,uuid(b.executionId,'executionId'),b)});}if(action==='corrective-action'){if(!permitted(context,'compliance.manage'))throw err(403,'FORBIDDEN','You do not have permission for this operation.');const result=await repository.writeCorrectiveAction(context,b.operation||'CREATE',b.actionId?uuid(b.actionId,'actionId'):null,Number(b.expectedVersion||0),b);if(result?.conflict)throw err(409,'VERSION_CONFLICT','Corrective action changed.');return res.status(201).json({data:result});}throw err(400,'UNSUPPORTED_ACTION','Unsupported Checklist action.');}catch(error){const{status,response}=errorEnvelope(error);return res.status(status).json(response);}}}
module.exports={createChecklistsHandler};
